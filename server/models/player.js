'use strict';

module.exports = function(Player) {

    /**
 * A search to the backend for music
 * @param {string} artist User submits the kind of user to listen to his track
 * @param {Function(Error, object)} callback
 */

    Player.search = function(artist, callback) {
        // var artist;
        // // TODO
        // callback(null, artist);
        console.log(artist)
    };
    
};
